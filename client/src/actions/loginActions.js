export const LOGIN = 'LOGIN';
export const LOGOUT = 'LOGOUT';

// TODO: success states

export const login = (userInfo) => {
  return dispatch => {

  };

  // type: LOGIN,
  // userInfo,
}

// TODO: in future, join user and login together

export const logout = () => ({
  type: LOGOUT,
})
